import React from 'react'
import { Layout } from 'antd'

import styles from './styles.module.scss'

const Footer = () => <Layout.Footer className={styles.footer}>Application template footer</Layout.Footer>

export default Footer
